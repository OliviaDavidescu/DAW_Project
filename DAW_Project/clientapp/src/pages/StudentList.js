import StudentsList from "../components/StudentsList";

const Students = () => {
    return (
        <div className="appbar">
            <StudentsList />
        </div>
    );
}

export default Students;